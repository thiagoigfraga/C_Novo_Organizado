#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b, soma;

    printf("Digite dois numeros para somar: \n");
    scanf("%d", &a);
    scanf("%d", &b);
    soma = a + b;
    printf("A soma de %d e %d eh: %d \n", a,b,soma );
    return 0;
}
-------------------------------------------------------------------------------------------------------------------------------------------
